#include <stdio.h>
int main()
{
    int x;
    printf("Enter an integer number x: ");
    scanf("%i", &x);
    if ( x%2==0) {
        printf("%i is an odd number", x);
        }
    else {
            printf("%i is an odd number",x);
    }
    getch ();
}

